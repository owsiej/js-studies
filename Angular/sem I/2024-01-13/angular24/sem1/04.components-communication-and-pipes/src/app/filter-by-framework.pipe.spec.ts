import { FilterByFrameworkPipe } from './filter-by-framework.pipe';

describe('FilterByFrameworkPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByFrameworkPipe();
    expect(pipe).toBeTruthy();
  });
});
